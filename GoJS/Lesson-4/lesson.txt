https://operatino.github.io/MCSS/

ctrl+alt+l - отформатировать текст

РЕКУРСИЯ:
Рекурсивные функции.
Вызов функции из самой себя. Нужно выходить.

Сумма всех чисел от одного до Н

a - количество чисел, которіе

function sum(n){return n ? n + sum (--n : 0} - сумма чисел от n  до 1.
факториал - так же, только умножаем вместо суммирования.

n! = (n * (n-1) * (n-2)....)

ИМЕНОВАНЫЕ ФУНКЦЙИОНАЛЬНЫЕ ВЫРАЖЕНИЯ
Используется только для рекурсивных функций.

СТРУКТУРЫ ДАННЫХ, МЕТОДЫ И СВОЙСТВА.

Свойства - значения, методы.
text.toUppeCase()
text.toLowerCase()

n.toFixed(2) Оставит 2 цифрі после .






