https://operatino.github.io/MCSS/

ctrl+alt+l - отформатировать текст

РЕКУРСИЯ:
Рекурсивные функции.
Вызов функции из самой себя. Нужно выходить.

Сумма всех чисел от одного до Н

a - количество чисел, которіе

function sum(n){return n ? n + sum (--n : 0} - сумма чисел от n  до 1.
факториал - так же, только умножаем вместо суммирования.

n! = (n * (n-1) * (n-2)....)

ИМЕНОВАНЫЕ ФУНКЦЙИОНАЛЬНЫЕ ВЫРАЖЕНИЯ
Используется только для рекурсивных функций.

СТРУКТУРЫ ДАННЫХ, МЕТОДЫ И СВОЙСТВА.

Свойства - значения, методы.
text.toUppeCase()
text.toLowerCase()

n.toFixed(2) Оставит 2 цифрі после .

isNan - проверяем на нан

typeof NaN = number/

isFinite(n) - для чисел вернет тру, для инф и нан - фолс

+ перед строкой конвертирует в число.
+'   -12 ' dthytn -12

Правильно использовать:
ParseInt
ParseFloat - парсит число с .

toString - в строку в нужной системе исчисления.

Округление:
Math.floor
Math.ceil
Math.round
вниз, вверх, по правилам.

Неточніе вічисления!!!

Math.
aqrt
log
pow
abs
exp
max
min
random от 0 до 1
Math.ceil random ()

СТРОКИ
Класс STRING

CharAt

str[3] - третий символ строки

indexOf() - найти подстроку.

lastIndexOf - то же, с конца в начало

Задача - при вводе слова сделать его с заглавной буквы.

function upperFirst(str) {

   return str.length ? str[0].toUpperCase() + str.slice(1, str.length) : str;
}




